import React from 'react';
import { registerComponent } from "meteor/vulcan:core";

const PropertiesList = () =>
  <div >
    Find me at packages / zensroom / lib / components / properties / PropertiesList.jsx 
  </div>

registerComponent('PropertiesList', PropertiesList);

export default PropertiesList;