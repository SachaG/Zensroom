import assert from 'assert';
import resolvers from '../../../lib/models/properties/resolvers';

describe('The resolvers for: properties', () => {
  it('should have some tests', () => {
    assert(false);
  });
});