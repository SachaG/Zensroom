import assert from 'assert';
import parameters from '../../../lib/models/properties/parameters';

describe('The parameters for: properties', () => {
  it('should have some tests', () => {
    assert(false);
  });
});