import assert from 'assert';
import fragments from '../../../lib/models/properties/fragments';

describe('The fragments for: properties', () => {
  it('should have some tests', () => {
    assert(false);
  });
});