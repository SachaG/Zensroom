import assert from 'assert';
import schema from '../../../lib/models/properties/schema';

describe('The schema for: properties', () => {
  it('should have some tests', () => {
    assert(false);
  });
});