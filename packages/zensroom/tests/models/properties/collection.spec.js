import assert from 'assert';
import collection from '../../../lib/models/properties/fragments';

describe('The collection for: properties', () => {
  it('should have some tests', () => {
    assert(false);
  });
});