import assert from 'assert';
import mutations from '../../../lib/models/properties/mutations';

describe('The mutations for: properties', () => {
  it('should have some tests', () => {
    assert(false);
  });
});