import assert from 'assert';
import permissions from '../../../lib/models/properties/permissions';

describe('The permissions for: properties', () => {
  it('should have some tests', () => {
    assert(false);
  });
});