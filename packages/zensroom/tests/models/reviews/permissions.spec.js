import assert from 'assert';
import permissions from '../../../lib/models/reviews/permissions';

describe('The permissions for: reviews', () => {
  it('should have some tests', () => {
    assert(false);
  });
});