import assert from 'assert';
import collection from '../../../lib/models/reviews/fragments';

describe('The collection for: reviews', () => {
  it('should have some tests', () => {
    assert(false);
  });
});