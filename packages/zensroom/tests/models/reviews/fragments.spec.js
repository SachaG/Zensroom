import assert from 'assert';
import fragments from '../../../lib/models/reviews/fragments';

describe('The fragments for: reviews', () => {
  it('should have some tests', () => {
    assert(false);
  });
});