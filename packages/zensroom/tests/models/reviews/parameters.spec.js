import assert from 'assert';
import parameters from '../../../lib/models/reviews/parameters';

describe('The parameters for: reviews', () => {
  it('should have some tests', () => {
    assert(false);
  });
});