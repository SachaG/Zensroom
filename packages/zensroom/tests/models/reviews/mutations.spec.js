import assert from 'assert';
import mutations from '../../../lib/models/reviews/mutations';

describe('The mutations for: reviews', () => {
  it('should have some tests', () => {
    assert(false);
  });
});