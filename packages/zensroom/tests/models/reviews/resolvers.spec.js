import assert from 'assert';
import resolvers from '../../../lib/models/reviews/resolvers';

describe('The resolvers for: reviews', () => {
  it('should have some tests', () => {
    assert(false);
  });
});