import assert from 'assert';
import parameters from '../../../lib/models/bookings/parameters';

describe('The parameters for: bookings', () => {
  it('should have some tests', () => {
    assert(false);
  });
});