import assert from 'assert';
import mutations from '../../../lib/models/bookings/mutations';

describe('The mutations for: bookings', () => {
  it('should have some tests', () => {
    assert(false);
  });
});