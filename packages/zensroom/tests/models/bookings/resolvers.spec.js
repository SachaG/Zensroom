import assert from 'assert';
import resolvers from '../../../lib/models/bookings/resolvers';

describe('The resolvers for: bookings', () => {
  it('should have some tests', () => {
    assert(false);
  });
});