import assert from 'assert';
import permissions from '../../../lib/models/bookings/permissions';

describe('The permissions for: bookings', () => {
  it('should have some tests', () => {
    assert(false);
  });
});