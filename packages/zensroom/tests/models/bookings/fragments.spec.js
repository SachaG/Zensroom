import assert from 'assert';
import fragments from '../../../lib/models/bookings/fragments';

describe('The fragments for: bookings', () => {
  it('should have some tests', () => {
    assert(false);
  });
});