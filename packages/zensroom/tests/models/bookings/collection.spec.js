import assert from 'assert';
import collection from '../../../lib/models/bookings/fragments';

describe('The collection for: bookings', () => {
  it('should have some tests', () => {
    assert(false);
  });
});