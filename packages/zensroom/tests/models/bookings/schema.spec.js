import assert from 'assert';
import schema from '../../../lib/models/bookings/schema';

describe('The schema for: bookings', () => {
  it('should have some tests', () => {
    assert(false);
  });
});